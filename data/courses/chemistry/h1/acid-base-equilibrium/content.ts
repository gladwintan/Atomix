import { Content } from "@/types/type";

const lesson1: Content[] = [
  {
    type: "Notes",
    title: "Notes",
    description: "Acid Base equilibrium notes",
  },
];

const contents = {
  lesson1: lesson1,
};

export default contents;
