import { View, Text } from 'react-native'
import React from 'react'

const ReplyCard = ({
  id,
  parentId,
  postId,
  content,
  creationDate,
  lastUpdatedDate,
  author,
  isAuthor
}) => {
  return (
    <View>
      <Text>ReplyCard</Text>
    </View>
  )
}

export default ReplyCard